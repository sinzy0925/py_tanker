# Environment and secrets
.env
.env.local
.env.*.local

# Local IMO list (may be proprietary)
data/imo_list.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.python-version

# Playwright / Chrome CDP user profile (locked files; do not commit)
.chrome-cdp-profile/

# Pipeline outputs (regenerate with cdp0 / cdp1–cdp4)
ship_data/out.jsonl
ship_data/ship_details_prev.json
ship_data/ship_details_jp.json
ship_data/ship_details_jp_prev.json
ship_data/ship_moved/
ship_moved/
