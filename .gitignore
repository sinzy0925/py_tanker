# Environment and secrets
.env
.env.local
.env.*.local

# Local IMO list (may be proprietary)
data/imo_list.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.python-version

# Playwright / Chrome CDP user profile (locked files; do not commit)
.chrome-cdp-profile/

# Pipeline outputs (regenerate with cdp0 / cdp1–cdp4)
station0_all.json
ship_details.json
ship_details_prev.json
out.jsonl
ship_moved/
matches*.jsonl
wide_tankers.jsonl
